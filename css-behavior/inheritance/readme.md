# inheritance

## deifnicion tecnica

La herencia es el mecanismo mediante el cual ciertos valores de propiedades CSS se transmiten desde un elemento padre hacia sus elementos descendientes en el DOM cuando estos no tienen un valor explícito definido.
